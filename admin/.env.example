# Local dev (default if unset)
# VITE_API_URL=http://localhost:8080/v1

# Production API (Render)
VITE_API_URL=https://rozy.onrender.com/v1

# Vercel: add the same VITE_API_URL in Project → Settings → Environment Variables
